name: Cross-Browser Regression

# Reusable workflow — called by e2e.yml
# Accepts a 'browser' input: "chromium" | "firefox" | "webkit" | "all" (default)
on:
  workflow_call:
    inputs:
      browser:
        description: 'Browser(s) to test — "all" runs all three'
        required: false
        default: 'all'
        type: string
    secrets:
      BASE_URL:
        required: true
      USER_NAME:
        required: true
      PASSWORD:
        required: true
      API_URL:
        required: true
      USER_NAME_CHROMIUM:
        required: false
      PASSWORD_CHROMIUM:
        required: false
      USER_NAME_FIREFOX:
        required: false
      PASSWORD_FIREFOX:
        required: false
      USER_NAME_WEBKIT:
        required: false
      PASSWORD_WEBKIT:
        required: false

# ─────────────────────────────────────────────────────────────────────────────
# Jobs
#   1. setup        — resolves which browsers to test
#   2. test         — Playwright E2E, one parallel job per browser
#   3. merge-report — downloads per-browser blob reports, merges into one HTML
#
# Total elapsed-time budget: ≤ 60 minutes
#   setup (5 min) + test in parallel (45 min) + merge-report (10 min) = 60 min
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  # ── 1. Resolve browser matrix ─────────────────────────────────────────────
  # "all" (or empty)  →  run all three browsers in parallel
  # any single name  →  run only that browser
  setup:
    name: Resolve Browser Matrix
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      browsers: ${{ steps.resolve.outputs.browsers }}
    steps:
      - name: Determine which browsers to run
        id: resolve
        run: |
          if [[ "${{ inputs.browser }}" != "all" && -n "${{ inputs.browser }}" ]]; then
            echo 'browsers=["${{ inputs.browser }}"]' >> $GITHUB_OUTPUT
          else
            echo 'browsers=["chromium","firefox","webkit"]' >> $GITHUB_OUTPUT
          fi

  # ── 2. E2E Tests ──────────────────────────────────────────────────────────
  test:
    name: E2E — ${{ matrix.browser }}
    needs: setup
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        browser: ${{ fromJson(needs.setup.outputs.browsers) }}
    env:
      CI: true
      BASE_URL: ${{ secrets.BASE_URL }}
      USER_NAME: ${{ secrets.USER_NAME }}
      PASSWORD: ${{ secrets.PASSWORD }}
      API_URL: ${{ secrets.API_URL }}
      BROWSER: ${{ matrix.browser }}
      USER_NAME_CHROMIUM: ${{ secrets.USER_NAME_CHROMIUM }}
      PASSWORD_CHROMIUM: ${{ secrets.PASSWORD_CHROMIUM }}
      USER_NAME_FIREFOX: ${{ secrets.USER_NAME_FIREFOX }}
      PASSWORD_FIREFOX: ${{ secrets.PASSWORD_FIREFOX }}
      USER_NAME_WEBKIT: ${{ secrets.USER_NAME_WEBKIT }}
      PASSWORD_WEBKIT: ${{ secrets.PASSWORD_WEBKIT }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browser — ${{ matrix.browser }}
        # chromium is always required: the auth setup project has no browser override
        # and defaults to Chromium regardless of which test browser is selected
        run: npx playwright install chromium ${{ matrix.browser }} --with-deps

      - name: Run Playwright tests
        # --grep @ui: explicitly limits to UI-tagged tests.
        # API tests live in the 'api' project (no browser) and are already run
        # as a mandatory gate in api-tests.yml before this workflow starts.
        run: npx playwright test --project=${{ matrix.browser }} --grep @ui --workers=1 --reporter=blob

      - name: Upload blob report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: blob-report-${{ matrix.browser }}
          path: blob-report/
          retention-days: 2

      - name: Upload traces on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: traces-${{ matrix.browser }}
          path: test-results/
          retention-days: 2

  # ── 3. Merge blob reports into a single HTML report ───────────────────────
  merge-report:
    name: Merge & Publish Test Report
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: ${{ !cancelled() }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Download all blob reports
        uses: actions/download-artifact@v4
        with:
          pattern: blob-report-{chromium,firefox,webkit}
          path: all-blob-reports
          merge-multiple: true

      - name: Merge into single HTML report
        run: npx playwright merge-reports --reporter=html ./all-blob-reports

      - name: Upload merged HTML report
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 2
